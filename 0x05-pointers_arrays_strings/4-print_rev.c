#include "main.h"
/**
 * print_rev - prints reverse wordsd
 * @s: a parameter for the function 
 * Return: Always 0
 */
void print_rev(char *s)
{
	int a, k;
	int i = 0;

	for (i = 0; i != '\0'; i++)
	{
		i++
	}
	k = i;

	for (a = k; a >= 0; a--)
	{
		_putchar(a);
	}
	_putchar('\n');
}
