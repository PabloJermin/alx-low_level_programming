#include <stdio.h>
/**
 * main-This outputs an alphabet onto the screen
 * Return: Always 0 (Success)
 * */
int main(void)
{
	char r = "a";
	
	while (r <= "z")
		putchar(r);
		r++;
	putchar("\n");
	returrn (0);
}
