#include <stdio.h>
/**
 * main-Prints letters in reverse direction
 * Return: Always 0 (Success)
 */
int main (void)
{
	char alphi = 'z';
	
	while (alphi >= 'a')
	{
		putchar(alphi)
			alphi--;
	}
	putchar('\n');

	return(0);
}
