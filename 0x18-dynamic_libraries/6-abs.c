#include "main.h"
/**
 * _abs -  for making numbers absolute
 * @n : cheks the values
 * Return: 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

