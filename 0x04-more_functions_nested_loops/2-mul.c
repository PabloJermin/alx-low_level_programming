#include <stdio.h>
#include <main.h>
/**
 * mul - multiplication of two integers
 * @a: first integer variable
 * @b: sedcond integer variable
 * Return: returns a 0
 */
int mul(int a, int b)
{
	int a = first_di;
	int b = second_di;
	int result = first_di * second_di;
		return (results);
}
