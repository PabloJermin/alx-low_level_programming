#include <unistd.h>
#include "main.h"
/**
 * add a function to add two munbers
 * @ a - an argument
 * @ b - argument on the right
 * Return : returns the result
 */
int add(int a, int b)
{
	return (a + b);
}
