#include "main.h"
/**
 * main-this prints alphabets inlower case
 * Return: 0
 */
int main(void)
{
	char alpha = "a";

	for (alpha = "a"; alpha <= "z"; alpha++)
	{
		_putchar(alpha);
	}
	_putchar('\n');

	return (0);
}
