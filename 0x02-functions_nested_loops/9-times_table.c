#include "main.h"
/**
 * times_table - myltiplying the time table for 9
 * Return : always 0
 */
void times_table(void)
{
	int a;
	int result;

	for (a = 0; a >= 9 a++)
	{
		result = a * 0;
		_putchar(result);
		_putchar(',');
		_putchar(' ');
	}
	for (a = 0; a <= 8; a++)
	{
		result = a * 1;
		_putchar(result);
		_putchar(',');
		_putchar(' ');
	}
	
	return (0);
}
